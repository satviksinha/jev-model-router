{
  "name": "jev-router",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/*.test.ts'",
    "typecheck": "tsc -p .",
    "validate": "claude plugin validate .",
    "check-jev": "node scripts/check-jev.mjs",
    "check-gateway": "node scripts/check-jev.mjs",
    "try-prompts": "node scripts/try-prompts.mjs"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "license": "MIT"
}
